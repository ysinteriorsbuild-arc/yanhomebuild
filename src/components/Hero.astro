<section class="hero" id="top">
  <div class="hero-bg-photo" aria-hidden="true">
    <img
      src="https://images.pexels.com/photos/35058546/pexels-photo-35058546.jpeg?auto=compress&cs=tinysrgb&w=1920"
      alt=""
      width="1920"
      height="1280"
    />
  </div>
  <div class="hero-scrim" aria-hidden="true"></div>
  <div class="container hero-inner">
    <div class="hero-copy">
      <p class="eyebrow">Malaysia's Interior Design &amp; Build Studio</p>
      <h1>Spaces built<br />around how<br />you live.</h1>
      <p class="lede">
        We plan interiors and fabricate the furniture inside them —
        so design intent survives to installation.
      </p>
      <div class="hero-actions">
        <a href="#work" class="btn btn-primary">Explore Our Work</a>
        <a href="#contact" class="btn">Book a Consultation</a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 92vh;
    display: flex;
    align-items: flex-start;
    overflow: hidden;
    padding: 150px 0 80px;
  }

  .hero-bg-photo {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-bg-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  /* Dark gradient scrim so the hero copy stays legible over the photo,
     strongest on the left where the text sits and easing off to the
     right and top. */
  .hero-scrim {
    position: absolute;
    inset: 0;
    z-index: 1;
    background:
      linear-gradient(100deg, rgba(6, 12, 24, 0.95) 0%, rgba(6, 12, 24, 0.88) 42%, rgba(6, 12, 24, 0.55) 68%, rgba(6, 12, 24, 0.28) 100%),
      linear-gradient(to top, rgba(6, 12, 24, 0.55) 0%, rgba(6, 12, 24, 0) 30%, rgba(6, 12, 24, 0) 78%, rgba(6, 12, 24, 0.5) 100%);
  }

  .hero-inner {
    position: relative;
    z-index: 2;
  }

  .hero-copy { max-width: 620px; }
  .hero-copy h1 { margin-bottom: 28px; }
  .hero-copy .lede { max-width: 46ch; }

  .hero-actions {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    margin-top: 36px;
  }

  .hero-copy > * {
    animation: hero-rise 0.9s var(--ease) both;
  }

  .hero-copy > *:nth-child(1) { animation-delay: 0.05s; }
  .hero-copy > *:nth-child(2) { animation-delay: 0.14s; }
  .hero-copy > *:nth-child(3) { animation-delay: 0.23s; }
  .hero-copy > *:nth-child(4) { animation-delay: 0.32s; }

  @keyframes hero-rise {
    from { opacity: 0; transform: translateY(22px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-copy > * { animation: none; }
  }

  @media (max-width: 720px) {
    .hero { min-height: 92vh; padding: 100px 0 64px; }
    .hero-scrim {
      background:
        linear-gradient(180deg, rgba(6, 12, 24, 0.55) 0%, rgba(6, 12, 24, 0.72) 45%, rgba(6, 12, 24, 0.94) 100%);
    }
    .hero-copy { max-width: none; }
  }
</style>
