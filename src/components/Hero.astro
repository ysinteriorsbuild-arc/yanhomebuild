<section class="hero" id="top">
  <div class="hero-bg-photo" aria-hidden="true"></div>
  <div class="container hero-inner">
    <div class="hero-copy">
      <p class="eyebrow">Interior Design &amp; Custom Fabrication</p>
      <h1>Spaces built<br />around how<br />you live.</h1>
      <p class="lede">
        We plan interiors and fabricate the furniture and fixtures that go inside them —
        so design intent survives all the way to installation.
      </p>
      <div class="hero-actions">
        <a href="#work" class="btn btn-primary">Explore Our Work</a>
        <a href="#contact" class="btn">Book a Consultation</a>
      </div>
    </div>
    <div class="hero-visual gradient-border">
      <img
        src="https://images.pexels.com/photos/34952196/pexels-photo-34952196.jpeg?auto=compress&cs=tinysrgb&w=1000"
        alt="Luxurious dining room with custom wood paneling and cabinetry"
        width="1000"
        height="1250"
      />
    </div>
  </div>
</section>

<style>
  .hero { position: relative; padding: 56px 0 0; overflow: hidden; }

  /* A translucent rectangle of a real interior photo, centered over the
     hero, sized well past the section bounds and clipped by .hero's
     overflow: hidden. Sits at the very back of the hero's own stack —
     just in front of the global ambient ribbon/gradient background,
     behind the copy and hero-visual. Dissolves slightly at its
     bottom-left corner instead of ending on a hard edge. */
  .hero-bg-photo {
    display: none; /* temporarily disabled — remove this line to re-enable */
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: min(1920px, 186vw);
    aspect-ratio: 4 / 3;
    z-index: 0;
    pointer-events: none;
    background-image: url('/assets/hero-bg.jpg');
    background-size: cover;
    background-position: center;
    opacity: 0.25;
    -webkit-mask-image: linear-gradient(to top right, transparent 0%, #000 45%);
    mask-image: linear-gradient(to top right, transparent 0%, #000 45%);
  }

  .hero-inner {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 0.95fr 1.05fr;
    gap: 72px;
    align-items: center;
  }

  .hero-copy h1 { margin-bottom: 28px; }

  .hero-actions {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    margin-top: 36px;
  }

  .hero-copy > * {
    animation: hero-rise 0.9s var(--ease) both;
  }

  .hero-copy > *:nth-child(1) { animation-delay: 0.05s; }
  .hero-copy > *:nth-child(2) { animation-delay: 0.14s; }
  .hero-copy > *:nth-child(3) { animation-delay: 0.23s; }
  .hero-copy > *:nth-child(4) { animation-delay: 0.32s; }

  .hero-visual {
    aspect-ratio: 4 / 5;
    overflow: hidden;
    animation: hero-rise 1.1s var(--ease) 0.2s both;
  }

  @keyframes hero-rise {
    from { opacity: 0; transform: translateY(22px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-copy > *, .hero-visual { animation: none; }
  }

  .hero-visual img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  @media (max-width: 960px) {
    .hero-inner { grid-template-columns: 1fr; gap: 48px; }
    .hero-visual { order: -1; max-width: 460px; margin: 0 auto; width: 100%; }
    .hero-bg-photo { width: 85vw; }
  }

  @media (max-width: 560px) {
    .hero { padding: 32px 0 0; }
  }
</style>
